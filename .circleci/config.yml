version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
  test:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: test
          command: |
            npm install
            npm run prepare
            npm test
workflows:
  version: 2
  Example_Workflow:
    jobs:
      - build
      - test:
          requires:
            - build
