#!/bin/bash

# Iterate over all files in the current directory, including hidden files
for file in * .* ; do
    # Check if the item is actually a file
    if [ -f "$file" ]; then
        mv "$file" "$file.baseline"
    fi
done