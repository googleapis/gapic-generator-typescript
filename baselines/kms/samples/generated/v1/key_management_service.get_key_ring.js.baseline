


'use strict';

function main() {
  // [START kms_get_key_ring_sample]
  /**
   * TODO(developer): Uncomment these variables before running the sample.
   */
  /**
   *  The [name][google.cloud.kms.v1.KeyRing.name] of the [KeyRing][google.cloud.kms.v1.KeyRing] to get.
   */
  // const name ='abc123'

  // Imports the Kms library
  const {KeyManagementServiceClient} = require('');

  // Instantiates a client
  const kmsClient = new KeyManagementServiceClient();

  async function getKeyRing() {
    // Construct request
    const request = {
    };

    // Run request
    const response = await kmsClient.getKeyRing(request);
    console.log(`Response: ${response}`);
  }

  getKeyRing();
  // [END kms_get_key_ring_sample]
}

process.on('unhandledRejection', err => {
  console.error(err.message);
  process.exitCode = 1;
});
main(...process.argv.slice(2));
