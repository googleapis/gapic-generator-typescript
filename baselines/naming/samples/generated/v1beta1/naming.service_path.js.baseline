


'use strict';

function main() {
  // [START naming_service_path_sample]
  /**
   * TODO(developer): Uncomment these variables before running the sample.
   */

  // Imports the Naming library
  const {NamingClient} = require('naming');

  // Instantiates a client
  const namingClient = new NamingClient();

  async function servicePath() {
    // Construct request
    const request = {
    };

    // Run request
    const response = await namingClient.servicePath(request);
    console.log(`Response: ${response}`);
  }

  servicePath();
  // [END naming_service_path_sample]
}

process.on('unhandledRejection', err => {
  console.error(err.message);
  process.exitCode = 1;
});
main(...process.argv.slice(2));
