{
  "name": "test-application",
  "version": "0.0.1",
  "description": "Test application for generated client library.",
  "engines": {
    "node": ">=8.13.0"
  },
  "scripts": {
    "test": "c8 --reporter=lcov bash showcase-setup.sh",
    "codecov": "c8 --reporter=lcov mocha build/test && c8 report",
    "lint": "gts check",
    "clean": "gts clean",
    "fix": "gts fix"
  },
  "author": "Google LLC",
  "license": "Apache-2.0",
  "dependencies": {
    "showcase": "file:///../../.baseline-test-out",
    "@grpc/grpc-js": "^0.5.2"
  }
}
